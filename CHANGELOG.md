# 更新日志

## v2.0.3 (2026-09-18)

**修复**
- 🐛 `/原图` 现在会剥离 ImgBed 处理参数，还原「未处理原文件」直链后再发送：此前把历史里的地址原样重发，而会话历史中可能存的是带 `width` / `height` / `fallback` 的缩放地址，于是「原图」拿回的仍是压缩图。现已同时处理查询参数式（剥离 `width`/`height`/`fit`/`fallback`）与 Cloudflare Image Resizing 路径式（剥离 `/cdn-cgi/image/...` 前缀还原为 `/file/` 直链），路径按 URL 规则重新编码，避免中文/空格文件名被协议端截断
- 🐛 `/原图 文件名` 在会话历史中未匹配时，不再直接回复「没有找到」，而是退化为按文件名到图床直查原图；仅当探测为「确定不存在」时才提示未找到，并附上尝试过的完整直链，便于核对图床目录层级
- 🐛 探测结果判定收紧：**仅 404 / 410 视为「确定不存在」**，403（防盗链）、429（限流）、405（方法不允许）及任何网络异常一律视为「说不准」并照常发送，避免图床文件确实存在时误报未找到；探测先走 `HEAD`，语义不明确时降级为 `Range: bytes=0-0` 的单字节 `GET`
- 🐛 `/原图` 的文件名参数解析改用关键词定位（`find`），修复原正则 `原图\s*(.*)` 在消息带 `@` 前缀或引用时会捕获整条消息、把噪声当成文件名的问题；关键词缺失时返回空串而非整条消息
- 🐛 会话历史新登记一行别名键：当配文显示名与 URL 文件名不一致时，两个键指向同一原图直链，避免用户复制群内看到的文件名来 `/原图` 却提示没找到；历史键长度统一截断到 256 字符

**优化改进**
- 🛡️ 按文件名直查时拒绝一切可能拼出跨站或路径穿越的输入：绝对 URL、`..`、`//`、反斜杠、CR/LF/TAB 与空名；拼出的结果还必须落回 ImgBed 的 `/file/` 口径，避免图床域名配错时发出一条语法合法却指向别处的链接
- ⚡ 新增的 ImgBed URL 工具均为纯字符串处理，无额外网络请求与依赖；`/原图` 直查仅在会话历史未命中时才发起，命中历史时零额外开销
- 📖 `/原图` 在未配置图床域名、或文件名不像图床文件名时，给出可操作的配置提示而非静默退回「没找到」
- ✅ 单元测试扩展至 93 个：新增 ImgBed URL 识别与还原、文件名直查与安全校验、探测状态码三态判定，以及 `/原图` 历史未命中兜底、参数剥离、非法文件名拒绝等分支

## v2.0.2 (2026-09-13)

**修复**

- 🐛 `scaled-url` 在 QQ/OneBot 上改为缩放 URL 优先发送；缩放 URL 失败后再回退原 URL，最后进入本地字节回退，避免原图直传成功时压缩配置始终不生效

## v2.0.1 (2026-09-13)

**修复**
- 🐛 QQ 恢复 17:26 版本的原始 URL 优先直传策略：`scaled-url` 模式先直传未追加参数的原 URL，失败后再尝试官方缩放 URL，最后才进入本地字节回退，避免缩放 URL 下载 403 后直接落回易显示 1:1 的字节消息
- 🐛 OneBot 直传前会把与配置域名同域的 HTTP 媒体地址升级为 HTTPS，减少协议端下载时的 301 跳转
- 🐛 OneBot 直传 URL 失败（例如协议端下载图片返回 403）时，先由插件下载原图并按当前模式压缩后以字节消息回退发送；仅插件本地下载也失败时才回退 URL 消息链，避免连续两次远端下载失败导致消息发送失败
- ✅ 单元测试扩展至 78 个：新增 QQ 原始 URL 优先直传、原始 URL 失败后缩放 URL 重试、OneBot 直传失败后的本地字节回退、本地下载失败后的 URL 回退，以及 `original-url` 模式保持原图字节不压缩

## v2.0.0 (2026-09-13)

**重大变更**
- 💥 配置结构升级为官方 Schema 支持的嵌套结构：`imgbed.*`、`message.*`、`image.*`；`apiToken` 标记为 `secret`，`image.maxSide` 在运行时限制为 `1`–`4096`
- 💥 图片默认发送方式改为 `scaled-url`：优先使用 ImgBed 官方 `width` / `height` / `fallback=original` 参数发送等比缩放图，消息文案标注「已压缩」并附带 `/原图 文件名`
- 💥 `image.quality` 仅用于 `local-compress`；ImgBed 读取 API 不支持 `quality` 参数，`scaled-url` 不再附加该参数

**兼容迁移**
- 🔄 旧顶层配置会自动迁移为新嵌套配置，保留未识别配置项，并写回 AstrBot 插件配置文件；写回失败时恢复配置对象旧结构且本次运行继续使用内存迁移结果
- 🔄 旧 `imageSendMode=url` + `enableCompress=true/false` 分别映射为 `scaled-url` / `original-url`；旧 `imageSendMode=compress` + `enableCompress=true/false` 分别映射为 `local-compress` / `original-url`

**规范化**
- 📚 按 AstrBot 插件配置与消息发送规范、NapCat OneBot 图片消息段规范、CloudFlare ImgBed 随机图/读取 API 规范整理实现与 README
- 🖼️ `original-url` 直传原 URL；`local-compress` 沿用 Pillow 压缩链路；`/原图` 始终直传未修改原 URL 并保留「（原图）」标注
- 🛡️ AVIF/HEIC/SVG 等协议端难以解析宽高的格式在 `scaled-url` 下仍降级为本地压缩；非 aiocqhttp 平台或 OneBot 调用失败时回退标准消息链
- ✅ 单元测试扩展至 75 个：覆盖配置迁移写回与失败回滚、Schema 字段规范、URL 构造、三种发送模式与 `/原图` 行为

## v1.4.0 (2026-09-13)

**修复**
- 🐛 修复 QQ 图片聊天气泡显示 1:1：AstrBot 的 aiocqhttp 适配器会把所有图片消息段统一转成 base64，协议端解析宽高失败后以 1024×1024 占位。插件改用 OneBot 原生 `send_group_msg` / `send_private_msg` 直传图片 URL，由协议端自行下载并解析真实宽高，并透传 `self_id` 支持多账号路由

**优化改进**
- ⚡ 默认 `imageSendMode=url`，跳过插件侧下载、压缩与 base64 编码开销，图片发送延迟更低；非 aiocqhttp 平台或协议端调用失败时自动回退标准消息链，不影响消息可达性
- 🛡️ AVIF/HEIC/SVG 等协议端无法解析宽高的格式自动降级为下载转 JPEG；`/原图` 命令始终直传原图 URL，且与 `/随机图` 共用统一发送链路
- ⚙️ 新增配置项 `imageSendMode`（`url` / `compress`），原有 `enableCompress`、`compressMaxSide`、`compressQuality` 在 `compress` 模式下继续生效
- 📖 README 更新图片发送方式说明与常见问题 8，说明根因、回退链路与适用场景
- ✅ 单元测试扩展至 58 个：覆盖 OneBot 直传群聊/私聊、平台探测、调用失败回退、非常见格式降级、模式切换与 `/原图` 直传

## v1.3.1 (2026-09-13)

**优化改进**
- 🛡️ QQ 聊天气泡 1:1 问题加固：QQ 气泡的图片显示比例由消息元数据中的宽高决定，而经 AstrBot 发送的消息不带宽高、由协议端（NapCat 等）自行解析，解析失败会以 1024×1024 占位显示成 1:1。插件发送前按文件头魔数识别图片格式并写入日志，AVIF/HEIC/SVG 等协议端无法解析宽高的格式即使较小也自动转成 JPEG；无法解码时回退原图直发并输出 WARNING 提示
- 🔄 重编码前按 EXIF Orientation 将旋转烘进像素，竖拍照片方向在压缩后保持正确
- 📖 README 新增常见问题「为什么 QQ 聊天里图片气泡显示为 1:1」，含根因说明与处理建议（升级 NapCat 到最新版等）
- ✅ 单元测试扩展至 49 个：图片格式嗅探、非常见格式小图重编码、EXIF 方向保持

## v1.3.0 (2026-09-13)

**新增功能**
- 🗜️ 随机图片先压缩再发送：插件下载原图后经 Pillow 等比缩放（默认最长边 1920px）+ JPEG 重编码（默认质量 85），以 `Image.fromBytes` 字节流发送，明显缩短发送延迟；压缩图文案标注「已压缩」并附可直接复制的 `/原图 文件名` 命令示范
- 🖼️ 新增 `/原图` 命令：不带参数重发本会话最近一张随机图片的原图，支持 `/原图 文件名` 按文件名找回指定的某一张（忽略大小写、可省略扩展名、唯一子串匹配，多候选时列出提示）；原图消息文案标注「（原图）」
- ⚙️ 新增配置项 `enableCompress`（默认开启）、`compressMaxSide`（默认 1920）、`compressQuality`（默认 85，1-100）

**优化改进**
- 🛡️ 压缩流程健壮性：动图不重编码保留动画、原图 ≤200KB 跳过重编码避免画质损失、下载原图限 30MB、配置 Token 时仅在图片与图床同域时附带鉴权头；下载/压缩任何环节失败自动回退原图 URL 直发
- ⚡ Pillow 压缩在独立线程执行，不阻塞事件循环；Pillow 未安装时压缩自动禁用，其余功能不受影响
- ✅ 单元测试扩展至 43 个：压缩行为、下载上限与鉴权头、原图历史与匹配、发送回退路径

## v1.2.2 (2026-09-12)

**重大变更**
- 📛 按 AstrBot 官方插件命名规范，插件标识符由 `cloudflare_imgbed_random` 重命名为 `astrbot_plugin_cloudflare_imgbed_random`，GitHub 仓库同步更名为 `astrbot_plugin_cloudflare_imgbed_random`
- ⚠️ 已安装用户需卸载后重新安装本插件，插件配置会按新插件名重新保存

**调整**
- 📋 `metadata.yaml` 规范化：`description` 字段改为 `desc`，新增 `repo` 字段，`astrbot_version` 按官方示例以双引号包裹版本约束
- ✍️ 补全 `@register` 注册装饰器中的作者信息

**修复**
- 🐛 修复 `astrbot_version` 因去除引号导致 `metadata.yaml` 解析失败（YAML 将行首 `>` 视为块标量指示符），面板安装插件时报「metadata.yaml 格式错误 / Plugin repository validation failed」的问题

## v1.2.1 (2026-09-12)

**调整**
- 🔧 发送随机媒体时不再显示文件夹目录，只显示文件名

## v1.2.0 (2026-09-12)

**新增功能**
- 🏷️ 发送随机图片/视频时附带所在的文件夹目录和文件名（自动从媒体 URL 解析，兼容中文及 URL 编码路径）
- ⚙️ 新增配置项 `showFileInfo`，可关闭附带信息

## v1.1.0 (2026-08-23)

**修复与改进**
- 🔧 修复 AstrBot 插件配置注入和命令注册方式
- 🔧 修复 LLM 工具目录参数丢失、`retryCount=0` 不请求等问题
- 🌐 支持相对 URL、直接返回媒体响应和更多媒体后缀
- 🛡️ 增加 URL、Token、响应体大小和内容类型校验，避免暴露内部异常
- ⚡ 复用 HTTP 会话并加入指数退避，减少重复连接开销
- ✅ 增加单元测试和 GitHub Actions 持续集成

## v1.0.0 (2026-03-22)

**新增功能**
- ✨ 支持从 CloudFlare ImgBed 获取随机图片和视频
- ✨ 支持 API Token 鉴权
- ✨ 支持指定目录获取媒体
- ✨ 支持 LLM 工具调用
- ✨ 支持多种命令格式
